# VELORA Frontend

React + TypeScript + Vite frontend for smartphone voice recording/upload and VELORA cognitive risk result display.

## Run

```bash
npm ci
npm run dev
```

Set the backend URL in `.env` when the API is on a different host:

```bash
VITE_API_URL=http://localhost:8000
```

## Build

```bash
npm run build
```
